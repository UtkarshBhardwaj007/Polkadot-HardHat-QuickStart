{
    "name": "Polkadot Hardhat Development",
    "image": "ghcr.io/utkarshbhardwaj007/polkadot-hardhat-quickstart:latest",
    "workspaceMount": "source=${localWorkspaceFolder},target=/project,type=bind",
    "workspaceFolder": "/project",
    "overrideCommand": true,
    "updateRemoteUserUID": false,
    "containerEnv": {
        "WORKSPACE_DIR": "/workspace",
        "PROJECT_DIR": "/project"
    },
    "postCreateCommand": "bash -c 'cd /project && cp -r /workspace/* /workspace/.[^.]* . 2>/dev/null || true && npm install && mkdir -p binaries && cd binaries && echo \"#!/bin/bash\" > substrate-node && echo \"echo substrate-node placeholder\" >> substrate-node && chmod +x substrate-node && echo \"#!/bin/bash\" > eth-rpc && echo \"echo eth-rpc placeholder\" >> eth-rpc && chmod +x eth-rpc'",
    "remoteUser": "root",
    "customizations": {
        "vscode": {
            "extensions": [
                "NomicFoundation.hardhat-solidity",
                "esbenp.prettier-vscode",
                "dbaeumer.vscode-eslint"
            ],
            "settings": {
                "terminal.integrated.defaultProfile.linux": "bash",
                "editor.formatOnSave": true,
                "editor.defaultFormatter": "esbenp.prettier-vscode",
                "solidity.formatter": "prettier"
            }
        }
    }
}
