{
    "name": "Polkadot Hardhat Development",
    "image": "ghcr.io/utkarshbhardwaj007/polkadot-hardhat-quickstart:latest",
    "overrideCommand": false,
    "postCreateCommand": "bash -c 'if [ -d /workspace ] && [ ! -f package.json ]; then cp -r /workspace/* /workspace/.gitignore . 2>/dev/null || true; fi && if [ -f package.json ] && [ ! -d node_modules ]; then npm install; fi'",
    "remoteUser": "root"
}
